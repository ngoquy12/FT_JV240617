// export const PI = Math.PI;

// export const GET = "get";

// export const POST = "post";

// export const sum = (a, b) => {
//   return a + b;
// };

const PI = Math.PI;

const GET = "get";

const POST = "post";

const sum = (a, b) => {
  return a + b;
};

export { PI, GET, POST, sum };
